# Student portal + admin panel (src/lib/portalClient.js).
# Talks to the SAME Supabase project as the Drill Finder. The publishable
# (anon) key is safe to expose in client code — Row Level Security protects the
# data. Set these in Vercel for production; the client falls back to the drill
# project's values when they're absent.
VITE_SUPABASE_URL=https://dtlcpjzxuamkplhpylin.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_8iUvTj_0U41R66rHfK0R-Q_6ngLtuK1

# Drill Finder (src/lib/drillClient.js) — same project; override only if it ever
# moves to its own project again.
# VITE_DRILL_SUPABASE_URL=
# VITE_DRILL_SUPABASE_ANON_KEY=
